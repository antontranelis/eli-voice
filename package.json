{
  "name": "eli-voice",
  "version": "0.1.0",
  "private": true,
  "description": "Eli's Stimme im Redekreis — Live-Transkription, Claude AI, TTS",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "dev:web": "pnpm --filter @eli-voice/web dev",
    "dev:server": "pnpm --filter @eli-voice/server dev"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild", "onnxruntime-node"]
  }
}
